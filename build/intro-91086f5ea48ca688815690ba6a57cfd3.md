# Welcome

This Jupyter Book uses **MyST Markdown** and can be built locally or deployed to **GitHub Pages** using the included workflow.

```{note}
You can add chapters by creating more `.md` or `.ipynb` files and referencing them in `_toc.yml`.
```
